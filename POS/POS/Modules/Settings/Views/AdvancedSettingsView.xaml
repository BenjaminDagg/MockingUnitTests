<UserControl x:Class="POS.Modules.Settings.Views.AdvancedSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS.Modules.Settings.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1000">
    <Viewbox
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Width="{Binding SystemParameters.PrimaryScreenWidth}"
        Height="{Binding SystemParameters.PrimaryScreenHeight}">

        <Grid  VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            <Grid.RowDefinitions>
                <RowDefinition Height="{Binding SystemParameters.PrimaryScreenHeight}"/>
                <RowDefinition Height="{Binding SystemParameters.PrimaryScreenHeight}"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2045"/>
            </Grid.ColumnDefinitions>

                <local:PrinterSettingsView DataContext="{Binding PrinterSettingsViewModel}"/>

                <local:DeviceManagementSettingsView Grid.Row="1" DataContext="{Binding DeviceManagementSettingsViewModel}"/>

            
        </Grid>
    </Viewbox>
    
</UserControl>
