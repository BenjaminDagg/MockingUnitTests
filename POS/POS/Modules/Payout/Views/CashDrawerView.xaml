<UserControl x:Class="POS.Modules.Payout.Views.CashDrawerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="5" MinWidth="220">
        <TextBlock Text="{Binding CashDrawerViewModel.StartingBalance, StringFormat='Starting Balance: {0:C}'}" />
        <TextBlock Text="{Binding CashDrawerViewModel.CurrentBalance, StringFormat='Current Balance: {0:C}'}" />
        <TextBlock Text="{Binding CashDrawerViewModel.CashAdded, StringFormat='Cash Added: {0:C}'}" />
        <TextBlock Text="{Binding CashDrawerViewModel.CashRemoved, StringFormat='Cash Removed: {0:C}'}" />
        <TextBlock Text="{Binding CashDrawerViewModel.TotalPayout, StringFormat='Total Payout: {0:C}'}" />

        <Button HorizontalAlignment="Stretch" Command="{Binding CashDrawerViewModel.AddRemoveCashCommand}" CommandParameter="false" Content="Add Cash" Margin="0, 10, 0, 10" />
        <Button HorizontalAlignment="Stretch" Command="{Binding CashDrawerViewModel.AddRemoveCashCommand}" CommandParameter="true" Content="Remove Cash" Margin="0, 0, 0, 0"/>

    </StackPanel>
</UserControl>
