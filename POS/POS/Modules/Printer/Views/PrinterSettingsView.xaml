<UserControl x:Class="POS.Modules.Printer.Views.PrinterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Framework.WPF.UI.Controls;assembly=Framework.WPF.UI"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <controls:ExtendedScreenControl MinWidth="800"
                                    MinHeight="400"
                                    Margin="3"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch">
        <StackPanel Margin="10">
        <TextBlock Text="Report Printer" Margin="0,5,0,0" />
        <ComboBox Width="400" HorizontalAlignment="Left"  ItemsSource="{Binding Printers}" SelectedItem="{Binding ReportPrinter}" />
        <TextBlock Text="Receipt Printer" Margin="0,5,0,0" />
        <ComboBox  Width="400" HorizontalAlignment="Left" ItemsSource="{Binding Printers}" SelectedItem="{Binding ReceiptPrinter}" />
      
            <Button Margin="10, 20, 0, 0" x:Name="Save" Content="Save" />
       
    </StackPanel>
        </controls:ExtendedScreenControl>
</UserControl>
