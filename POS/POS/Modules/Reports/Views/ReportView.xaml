<UserControl x:Class="POS.Modules.Reports.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:cal="http://caliburnmicro.com"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:report="clr-namespace:BoldReports.UI.Xaml;assembly=BoldReports.Wpf"
             xmlns:controls="clr-namespace:Framework.WPF.UI.DGMaterial.Theme.Controls;assembly=Framework.WPF.UI.DGMaterial.Theme"
             xmlns:resx="clr-namespace:POS.Core;assembly=POS.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <DockPanel>
        <controls:CustomButtonControlEx x:Name="Back" DockPanel.Dock="Bottom" 
                                        Content="{x:Static resx:POSResources.UIReportNavigateBackText}"
                                        FontSize="16"  Width="150" Margin="20, 10, 20, 20"
                                        Foreground="{StaticResource DgWhite}"
                                        Background="{StaticResource DgDefaultButtonBackground}"
                                        HorizontalAlignment="Right"
                                        Height="36"/>

        <report:ReportViewer x:Name="reportViewer" cal:Message.Attach="[Event Loaded] = [Action HandleLoaded($eventArgs)]"/>

    </DockPanel>
</UserControl>
